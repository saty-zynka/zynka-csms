networks:
  default:
    name: zynka-csms
    external: true

services:
  solidstudio-vcp:
    # Replace with actual SolidStudio VCP Docker image when available
    # image: solidstudio/vcp:latest
    image: node:18-alpine  # Placeholder - replace with actual VCP image
    command: >
      sh -c "
        apk add --no-cache nodejs npm curl &&
        echo 'SolidStudio VCP simulator placeholder - replace with actual VCP image'
      "
    environment:
      - CS_ID=cs001
      - GATEWAY_URL=ws://gateway:9311/ws/cs001
      - AUTH_TYPE=basic
      - CS_USERNAME=cs001
      - CS_PASSWORD=password
    networks:
      - default
